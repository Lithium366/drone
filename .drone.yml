kind: pipeline
name: default

steps:
  - name: lint
    image: node:12-alpine
    commands:
      - npm ci
      - npm run lint
  - name: test
    image: node:12-alpine
    commands:
      - npm ci
      - npm run test
trigger:
  ref:
    - refs/pull/*/head
