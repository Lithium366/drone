kind: pipeline
name: default

steps:
  - name: build
    image: node:12-alpine
    commands:
      - npm ci
  - name: lint
    commands:
      - npm run lint
  - name: test
    commands:
      - npm run test
trigger:
  ref:
    - refs/pull/*/head
