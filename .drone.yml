kind: pipeline
name: default

steps:
  - name: build
    image: node:12-alpine
    commands:
      - npm ci
  - name: lint
    image: build
    commands:
      - npm run lint
  - name: test
    image: build
    commands:
      - npm run test
trigger:
  ref:
    - refs/pull/*/head
